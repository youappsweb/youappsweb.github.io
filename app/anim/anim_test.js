'use strict';

describe('myApp.anim module', function() {

  beforeEach(module('myApp.anim'));

  describe('anim controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var animCtrl = $controller('AnimCtrl');
      expect(animCtrl).toBeDefined();
    }));

  });
});