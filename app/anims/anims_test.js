'use strict';

describe('myApp.anims module', function() {

  beforeEach(module('myApp.anims'));

  describe('anims controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var animsCtrl = $controller('AnimsCtrl');
      expect(animsCtrl).toBeDefined();
    }));

  });
});